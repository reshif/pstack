{
  "id": "webhook-durable-idempotence",
  "description": "Make the purchase webhook's welcome-email dedupe survive a restart and hold across worker processes.",
  "source": "git show 30b9dfb:audits/2026-09-10-bugfix-run-736701de.md",
  "fixture": {
    "repo": "~/testpstack",
    "commit": "f13dd84",
    "strip_committed_pstack": true
  },
  "prompt": "_claimed is process memory, so a restart or a second worker re-welcomes every purchase it has not seen. Fix it so idempotence survives a restart and holds across workers.",
  "playbook": "bug-fix",
  "time_budget_seconds": 3600,
  "oracle": {
    "command": ["python3", "{evals}/oracle/webhook/oracle.py", "--tree", "{tree}", "--json", "{out}/oracle.json"],
    "result_json": "{out}/oracle.json",
    "time_budget_seconds": 1800,
    "checks": ["restart", "workers", "crash_recovery", "slow_send", "clock_step", "mark_sent_outage",
               "release_outage", "failed_send_retry", "redeploy", "read_only_install"]
  },
  "harness_replay": {
    "command": ["python3", "run_tests.py"],
    "time_budget_seconds": 180
  }
}
